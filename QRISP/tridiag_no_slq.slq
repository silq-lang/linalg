import QRISP.pauli_lcu;

def tridiag(anc:𝔹^2,qs:𝔹^2) mfree{
    coef := [0.5,-0.25,-0.125,-0.125]:!ℝ^4;
    ind := [(0,0),(1,0),(3,0),(3,3)]:(!ℕ^2)^4;
    return pauli_LCU[2,2,4](anc,qs,ind,coef);
}

def main(){
    return tridiag(0:uint[2] as 𝔹^2,0:uint[2] as 𝔹^2);
}